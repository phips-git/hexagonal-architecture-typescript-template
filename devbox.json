{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.18.3/.schema/devbox.schema.json",
  "packages": ["nodejs@24"],
  "shell": {
    "init_hook": ["unset SDKROOT DEVELOPER_DIR TOOLCHAINS"],
    "scripts": {
      "clean": "nix store gc --extra-experimental-features nix-command"
    }
  }
}
